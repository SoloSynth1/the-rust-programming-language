use std::io;

fn main() {
    let mut input = String::new();
    io::read_line(&mut input)
        .trim()
        .parse()
        .expect("Please enter a number!")

    let mut mode = String::new();

}
